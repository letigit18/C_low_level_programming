#include "main.h"

/**
 * mul - the functions that multiplies two numbers
 * @a: the first parameter
 * @b: the second parameter
 * Return: the multiplication of two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
