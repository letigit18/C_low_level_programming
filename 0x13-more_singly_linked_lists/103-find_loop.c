#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
 * find_listint_loop - the function that find the list
 * @head: the head node address
 * Return: the address of the node
 */
listint_t *find_listint_loop(listint_t *head)
{

