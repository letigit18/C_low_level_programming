#include <stdio.h>

/**
 * main - function main
 * description - beginning of the execution
 * Return: (0) returns 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
